import React from 'react';
import MainNav from '../../components/mainNav'; // Import the mainNav component

const Page: React.FC = () => {
    return (
        <div>
            <MainNav /> // Use the mainNav component as a JSX element
        </div>
    );
};

export default Page;